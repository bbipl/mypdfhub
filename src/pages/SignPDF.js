import React from "react";

const SignPDF = () => {
  return (
    <div>
      <h1>Merge PDF Tool</h1>
      <p>This is the page where you can merge PDF files.</p>
    </div>
  );
};

export default SignPDF;
